import SyncLoader from 'react-spinners/SyncLoader';

const Loader = () => {
  return <SyncLoader className="my-10 flex items-center justify-center" color="#235091" size={20} margin={8} />;
};

export default Loader;
